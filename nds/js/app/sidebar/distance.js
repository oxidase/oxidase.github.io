define(['knockout'], function(ko) {

    function DistanceViewModel(map) {

        this.activate = function () {
        }
        this.deactivate = function () {
        }
    }

    return DistanceViewModel;
});
